# lumesh 近期版本动态（0.16.13 → 0.17.5）

承接上期报告（0.14.x–0.16.12：编辑器重写、分词器重构、History/Slash Commands、AI 集成），本期焦点转向语言核心层——**字符串/引号系统的深度重构**、**`Bytes` 类型的引入**、**后台任务管理体系的建立**，以及一系列贯穿始终的性能打磨。

---

### 字符串与引号语义：本期核心变更

这是本期分量最重的一组改动，历经 0.16.13 → 0.17.0 → 0.17.5 三个版本的持续演进，最终收敛为一套完整、自洽的转义语义体系：

- **0.16.13**：`rewrite unescape for all string`——重写所有字符串类型的转义逻辑，为后续引号语义的精细化划分铺平道路。
- **0.17.0**：**引入 `StringSafe`**（`s'...'`），专为命令拼接场景而生，从语言层面杜绝注入风险：
  ```bash
  let s = s'(rm -rf /)'
  let s2 = into.safe 'mkfs /dev/sda'
  eval_str(`echo $s $s2`)  # never unsafe eval
  ```
  同期**引入 `SymbolRaw`**，令 `^` 转义符的适用范围从命令位置扩展到参数位置。
- **0.17.5**：**引入 hashed raw quote（`#` 包裹语法）**，正式确立多档次的转义梯度：
  ```rust
  r#'....'#        // 完全不转义
  r##'....'#′      // 支持任意数量的 #（应对内容自身含 # 的情形）

  r#"...."#        // 不转义引号，但转义 ansi 与 unicode
  t#"...."#        // g/t/s/b 前缀均不做转义
  ```
  **破坏性变更**：`r'....'` 不再承担 regex 前缀的职责，转而收窄为纯粹的 **raw string** 语义；原有的 **regex 前缀正式更名为 `g'....'`**。

  这一设计巧妙地用 `#` 分隔符化解了"边界符与内容含引号"的天然冲突，同时让"是否转义 ANSI/unicode"与"是否转义引号"解耦为两个独立可组合的维度——最终呈现出一条从 `"..."`（全转义）经 `r#"..."#`（半转义，引号不转义、ANSI/unicode 仍转义）到 `r#'...'#`（全不转义）的完整语义梯度，层次分明，各司其职。

---

### `Bytes` 类型引入（0.17.0）

- 字面量支持：`b'\x41'`
- 支持切片、比较、运算符重载
- 支持打印、管道传递、作为命令参数使用
- **0.17.5**：语法进一步丰富，新增 `b"..."`（双引号写法）与 `b#"..."#`（`#` 包裹写法），与字符串体系的转义语义保持一致的书写风格。

---

### 数字字面量增强（0.17.0）

- 引入 **Radix 数字**：`0b100_100`、`0o170`、`0xff`
- 全数字类型统一支持 `_` 分隔符：`999_999`、`999_999.999_999`
- **0.17.5**：Radix 数字解析性能进一步优化。

---

### 后台任务与终端集成（0.17.3 起）

- **0.17.3**：新增 `jobs` 命令，用于统一管理后台任务。
- **0.17.5**：
  - PTY（伪终端）相关性能与稳定性优化
  - 修复管道输出到 `vi` 时的画面残留问题
  - 非 PTY 场景下补齐 `Ctrl+Z` 支持

---

### 语法/分词细节打磨（0.17.3）

- 自定义一元操作符的归类方式调整
- 改进 `.`、`^`、`+` 的分词处理精度
- 移除索引运算符 `@`
- 统一 `-`/`!` 前缀标记的分词逻辑
- 改进 `<<`，新增字节流支持
- **`?&` 更名为 `&:`**，延续 0.16.2 引入的短路逻辑写法风格
- **`let` 默认关闭 CFM**：避免 `let a=1` 这类语句被 CFM 误判为命令解析，同时保留 `dd if=/dev/sda` 这类地道的 CFM 写法不受影响
- 改进 `^` 与 `:` 前后空格的识别逻辑
- 修复 `symof`：`assert(symof(1+2), 'BinaryOp')`

---

### 标准库增强（0.17.3）

- `fs.ls` 支持直接指定文件（`fs.ls file(s)`）
- 内置库支持通配符展开

### 提示符集成（0.17.3）
- 与 starship 集成，提升 shell 提示符的可定制性

---

### 版本节奏小结

| 阶段 | 核心方向 |
|------|----------|
| 0.16.13 | 全字符串转义逻辑重写、CFM/自定义操作符修复 |
| 0.17.0 | `Bytes` 类型、`StringSafe`/`SymbolRaw`、Radix 数字、函数改名（Break Change） |
| 0.17.1–0.17.2 | 库函数/提示优化、`cfm auto` 模式、`&:` 短路语法 |
| 0.17.3 | `jobs` 后台任务管理、starship 集成、分词器精细化调整（Break Change：`?&` → `&:`） |
| 0.17.5 | **hashed raw quote 体系落地**（`r#'..'#`/`r#"..."#`/`g#'..'#` 等）、PTY/信号处理优化（**Break Change：`r'..'` 语义收窄为 raw string，regex 改为 `g'..'`**） |

---

## lumesh 近期版本动态（0.14.x → 0.16.x）

### 编辑器全面重写（0.15.0）

0.15.0 是近期最重要的里程碑：**从 rustyline 迁移至 crossterm，编辑器从零重写**。此后数个版本持续打磨：

- **0.15.1**：编辑器主题、缓冲区优化、自定义热键绑定
- **0.15.2**：多行编辑模式、输入验证、光标与提示位置修复
- **0.16.0**：修复 ONLCR 问题，路径补全排序优化
- **0.16.2**：新增 `ui.editor`、`ui.date_pick`，修复 CapsLock 识别、热键修饰符映射等多个 bug
- **0.16.10**：编辑器支持滚动

---

### 分词器重构（0.15.5 起）

- **0.15.5**：**重构分词器，引入 dispatch 机制**，按优先级分发标记解析，解析逻辑更清晰可维护
- **0.15.6**：改进 CFM（Command First Mode）符号处理，统一高亮逻辑
- **0.15.7**：修复尾部 `&` 的分词问题
- **0.16.2**：修复模块调用的分词器
- **0.16.7**：CFM 模式增强，改为取整个单词（避免如 `1.1.1.1` 被误解析为浮点数）
- **0.16.8**：改用静态 regex，提升分词性能

---

### History 系统持续演进

- **0.16.5**：新增历史提示（history hint），ESC 在多行模式下移至末尾并清除提示
- **0.16.8**：**引入 slash commands**（`/h`、`/hh`、`/hm` 等斜杠命令体系）
- **0.16.9**：更智能的历史记录权重与排序，新增 `/q` 快速退出命令
- **0.16.10**：
  - 优化 `Ctrl+R` 长历史记录的显示
  - 多行命令自动忽略 `/h...` 历史记录（避免霸屏）
  - `/h`、`/hh`、`/hm`、`/history` 支持前缀过滤

---

### 补全系统大幅增强（0.15.3–0.16.3）

- 路径补全、外部命令补全、参数感知补全逐步完善
- 支持 lumesh 脚本作为补全数据源
- 补全颜色与上下文感知
- `ui.pick`/`ui.multi_pick` 支持 `table`/`map` 类型输入
- `ui.float` 支持自定义小数位

---

### AI 集成深化

- **0.15.9**：`ALT+i` 触发 AI 提示
- **0.16.0**：`ALT+Enter` / `ALT+o` 触发 AI 生成
- **0.16.3**：默认启用 ai-tls，分离 ai-https 为可选特性
- **0.16.8**：更新 AI 技能配置
- **0.16.10**：更新 AI 文档

---

### 语言特性

- **0.14.0**：引入 `table` 表达式与内置 `table` 库
- **0.14.3**：改进引号语义（`''` 原始字符串 / `""` 普通转义 / ` `` ` 全转义+变量插值）
- **0.16.5**：新增 `continue` 语句；`match` 箭头支持换行；**破坏性变更：声明时只允许单值**
- **0.16.7**：`~` 在符号中自动展开，普通模式支持前缀匹配

---

### 标准库结构调整（0.16.10）

模块职责重新划分：

| 变更 | 说明 |
|------|------|
| `fs.dirs` → `sys` | 目录相关功能归入系统库 |
| `sys.print_tty` / `sys.discard` → `console` | 终端输出控制归入控制台库 |
| 删除 `sys.cds` | 清理冗余接口 |
| 修复浮点文件大小显示 | `float filesize` 格式化修复 |

---

### 阶段性总结

| 阶段 | 核心方向 |
|------|----------|
| 0.14.x | 语言特性完善（table、引号、bug 修复） |
| 0.15.x | 编辑器重写 + 分词器重构 + 补全系统建设 |
| 0.16.x | Slash commands 体系 + History 智能化 + AI 集成 + 模块重组 |
